apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: registro-atividades
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
    - protocol: TCP
      port: 8001
      targetPort: 80
  type: LoadBalancer
